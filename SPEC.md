# HY-World 專案 Spec

> 更新日期：2026/05/03  
> 這份文件是 Claude Code 的工作指引，每個 Task 完成後更新狀態。

---

## 專案概覽

HY 的像素城市管理介面。每個地點對應現實中的一個場景，角色（bot 分身）在裡面自由活動。  
世界地圖採俯視 RPG 風格，點擊建築可進入室內場景查看各 bot 狀態與資料。

**Repo：** `HY-World`（Public）  
**部署：** GitHub Pages（`docs/` 資料夾）  
**技術：** 純 HTML + Canvas + Vanilla JS，零框架，無需 build step  
**API 來源：** `https://telegram-bot-t82n.onrender.com`  
**存取控制：** 密碼鎖頁面（方案 A，見下方登入系統規格）

---

## 登入系統（密碼鎖）

Repo 為 Public，程式碼公開，但網站有密碼保護。

### 流程
1. 進入 `index.html` 先顯示**登入畫面**，地圖不渲染
2. 輸入正確密碼 → HY 小人走出來迎接（動畫 1.5 秒）→ 進入地圖
3. 密碼存入 `localStorage`，下次同裝置自動跳過登入
4. 登入畫面右上角小字「清除登入狀態」可重置

### 登入畫面設計
- 背景：夜晚像素城市天際線（靜態）
- 中央：像素風輸入框 + 「進入 HY World」按鈕
- 輸入框上方：HY 小人 idle 動畫等待
- 錯誤時：小人搖頭，輸入框抖動

### 密碼設定
- 密碼**不寫在 SPEC.md**，由 HY 自行決定
- 寫在 `index.html` 的 JS 常數：`const PASSWORD = 'your_password'`
- Repo 雖然 Public，但一般人不會去翻程式碼找密碼
- 若需要更高安全性，未來可改方案 B（Private + Vercel）

---

## 地圖佈局

```
╔══════════════════════════════════════════════════════╗
║  🏠 Home          🏢 總部 HQ          🔬 ITRI         ║
║  小因平時在此      HY 坐鎮             950157 上班      ║
╠══════════════════════════════════════════════════════╣
║       （橫向道路連通上排三棟）                          ║
║       （總部向下有道路通往下排）                        ║
╠══════════════════════════════════════════════════════╣
║  🚧 蛋糕店        🚧 成衣廠           🚧 美業工作室     ║
║  （建設中）        （建設中）           （建設中）        ║
╚══════════════════════════════════════════════════════╝
```

- Tile Size：20px
- 畫布：680 × 480px
- 道路：建築之間橫向連通，總部有縱向道路往下排

---

## 角色設定

| 角色 | 對應 Bot | 顏色 | 外觀特徵 | 平時位置 |
|------|----------|------|----------|----------|
| HY | 個人 HY（@HY_Host_Bot） | 綠色 | 戴眼鏡 | 總部・中央桌 |
| 小因 | 家庭（@HY_MyFamily_bot） | 粉色 | 長髮 | Home・客廳 |
| 950157 | ITRI 工作分身 | 藍色 | 工程師帽 | ITRI・工作站 |

### 角色行為

- **idle**：站立，輕微上下跳動（sin 動畫）
- **working**：坐在桌前，打字動畫（手部閃爍）
- **walking**：沿道路移動到目標地點（路徑規劃 A→B）
- **talking**：頭上顯示對話泡泡（白底圓角，2~4 秒淡出）
- **alert**：頭上出現驚嘆號（有新通知/待處理事項）

### 晨會觸發（Phase 2，未來實作）

`/morning` 指令 → 小因從 Home、950157 從 ITRI 沿道路走向總部 → 到齊後依序說話泡泡回報狀態

---

## 建設中場景規格

下排三棟（蛋糕店、成衣廠、美業）：
- 像素鷹架圖案（灰色條紋 + 斜桿）
- 「🚧 建設中」標牌
- 狀態燈：灰色
- 點擊觸發泡泡：「這裡還在規劃中...」
- 未來 bot 建立後，替換為正式場景 js 檔即可

---

## 室內場景規格

點擊上排任一建築進入，共同元素：
- 左上角「← 返回地圖」按鈕
- 像素室內環境（桌椅、植物、窗戶、地板磁磚）
- 對應角色在裡面活動
- 右側資料面板（從 Render API 讀取）

### Home 室內
- 場景：客廳 + 廚房
- 角色：小因（沙發或桌前）
- 資料面板：家庭近期行程、孩子注意事項（`family_data.json`）

### 總部 HQ 室內
- 場景：開放辦公室 + 會議桌
- 角色：HY（中央桌）
- 資料面板：Claude 跨域建議、三 bot 狀態摘要（`/api/overview`）

### ITRI 室內
- 場景：研究室 + 工作站
- 角色：950157（對著螢幕打字）
- 資料面板：專案進度、待辦任務（`progress_data.json`）
- 點擊 950157 小人或室內螢幕 → 展開現有 Dashboard 看板
  - 方式：iframe 嵌入 `https://telegram-bot-t82n.onrender.com/dashboard`
  - 或：直接在右側面板重現進度追蹤頁籤內容（純讀取）

---

## API 串接

```javascript
const API_BASE = 'https://telegram-bot-t82n.onrender.com'
const SECRET   = '950157secret'  // X-Secret header

// 取得總覽（Claude 跨域建議）
GET  /api/overview

// 取得個人資料
GET  /api/personal-data

// 取得家庭資料
GET  /api/family-data

// 取得 950157 進度
GET  /api/progress
```

> 注意：以上 endpoint 部分尚未在 Render 實作，Claude Code 實作 Task 11 時需同步確認後端是否存在。

---

## 檔案結構

```
HY-World/
├── index.html              ← 世界地圖入口（主畫布）
├── SPEC.md                 ← 本文件
├── engine/
│   ├── world.js            ← 地圖渲染、Tile 系統、場景切換
│   ├── character.js        ← Character 類別（移動/動畫/對話泡泡）
│   └── api.js              ← Render API 串接工具
├── scenes/
│   ├── home.js             ← Home 室內場景
│   ├── hq.js               ← 總部室內場景
│   ├── itri.js             ← ITRI 室內場景
│   └── construction.js     ← 建設中場景（共用）
├── data/
│   └── world.json          ← 地點定義、角色初始位置、建築 metadata
└── sprites/
    └── characters.js       ← 所有角色像素圖定義（pixel art arrays）
```

---

## Claude Code 任務清單

| Task | 內容 | 狀態 |
|------|------|------|
| 01 | Repo 初始化、`index.html` 骨架、GitHub Pages 設定（`docs/`） | ✅ 完成 |
| 02 | 登入畫面：像素風密碼輸入、localStorage 記住登入、錯誤動畫 | ✅ 完成 |
| 03 | 世界地圖底層：地板 tile、道路、建築外觀（含建設中鷹架） | ✅ 完成 |
| 04 | `Character` 類別：sprite 渲染、idle 動畫、位置系統 | ✅ 完成 |
| 05 | 三個角色 sprite：HY（綠眼鏡）、小因（粉長髮）、950157（藍工程師帽） | ✅ 完成 |
| 06 | 角色移動動畫：沿道路 A→B 路徑走路 | ✅ 完成 |
| 07 | 對話泡泡系統：點擊角色觸發，2~4 秒淡出 | ✅ 完成 |
| 08 | 點擊建築進入室內場景框架（場景切換機制） | ✅ 完成 |
| 09 | Home 室內場景（客廳、小因在場） | ✅ 完成 |
| 10 | 總部 HQ 室內場景（辦公桌、會議桌、HY 在場） | ✅ 完成 |
| 11 | ITRI 室內場景（工作站、950157 在場、串接現有 Dashboard） | ✅ 完成 |
| 12 | API 串接（Render），室內場景資料面板顯示 | ✅ 完成 |
| 13 | GitHub Pages 部署驗收、CORS 確認 | ⬜ 未開始 |

---

## 未來擴充（Phase 2+）

- 蛋糕店（樂川 bot）
- 成衣廠（熊信 bot）
- 美業工作室（美業 bot）
- 晨會動畫（`/morning` 觸發三人走向總部）
- 夜間模式（21:00 後場景燈光變暗）
- 季節變化（場景背景隨季節更換）
- 角色對話互動（點 HY 問問題，泡泡顯示 AI 回答）

---

## 開發注意事項

1. **零依賴**：不使用任何框架或 npm 套件，確保 GitHub Pages 直接部署
2. **可擴展**：新場景只需新增 `scenes/新場景.js` + 更新 `world.json`
3. **API 降級**：Render 冷啟動可能需要 10~30 秒，UI 需顯示 loading 狀態
4. **像素風一致**：所有圖形用 Canvas 純手繪，`image-rendering: pixelated`
5. **Mobile 友善**：畫布支援 touch 事件（點擊建築、角色）
